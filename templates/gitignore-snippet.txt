# --- duetcode pipeline (install.js가 추가) ---
# duetcode는 yaml devDependency를 쓰므로 설치 절차에 npm install이 포함된다 — 커밋 금지
node_modules/
# 핸드오프 런타임 상태(세션 id·PID·lock·run 산출물) — 커밋 금지
tools/handoff/state/
# 검증 하니스 로컬 비밀값(계정·recordId) — 커밋 금지
tools/task/verify.local.json
